'use client'
import PublicNav from '@/components/PublicNav'
import { useState } from 'react'
export default function AirtimeCredit(){const[form,setForm]=useState({network:'MTN',product:'Airtime Purchase'});const[msg,setMsg]=useState(null);const set=(k,v)=>setForm({...form,[k]:v});async function submit(e){e.preventDefault();const r=await fetch('/api/financial-service-request',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...form,type:'airtime_credit'})});const b=await r.json();setMsg(`Request ${b.request_no} captured for airtime/recharge credit review.`)}return <><PublicNav/><main className="mx-auto max-w-5xl px-5 py-10"><a href="/services" className="font-black text-purple-800">← Back to Services</a><form onSubmit={submit} className="card mt-6 p-8"><p className="font-black uppercase tracking-widest text-purple-700">Airtime & Recharge Credit</p><h1 className="mt-2 text-4xl font-black text-purple-950">Airtime sales and recharge-card lending</h1><p className="mt-3 text-slate-600">Register airtime transactions, recharge-card borrowing, agent limits, repayment tracking and customer credit behaviour.</p><div className="mt-8 grid gap-4 md:grid-cols-2"><Field label="Customer / Agent Name" v={form.customer} set={v=>set('customer',v)} req/><Field label="Phone Number" v={form.phone} set={v=>set('phone',v)} req/><Select label="Network" v={form.network} set={v=>set('network',v)} opts={['MTN','Airtel','Glo','9mobile','Other']}/><Select label="Product" v={form.product} set={v=>set('product',v)} opts={['Airtime Purchase','Data Bundle','Recharge Credit / Borrowing','Agent Stock Advance']}/><Field label="Amount / Credit Limit" v={form.amount} set={v=>set('amount',v)} ph="10000"/><Field label="Repayment Due Date" v={form.due_date} set={v=>set('due_date',v)} ph="YYYY-MM-DD"/></div><button className="btn btn-gold mt-6">Create Airtime Credit Request</button>{msg&&<p className="mt-4 rounded-xl bg-emerald-50 p-4 font-bold text-emerald-800">{msg}</p>}</form></main></>}function Field({label,v,set,ph='',req=false}){return <div><label className="text-sm font-black">{label}</label><input className="input mt-2" value={v||''} onChange={e=>set(e.target.value)} placeholder={ph} required={req}/></div>}function Select({label,v,set,opts}){return <div><label className="text-sm font-black">{label}</label><select className="input mt-2" value={v} onChange={e=>set(e.target.value)}>{opts.map(o=><option key={o}>{o}</option>)}</select></div>}
