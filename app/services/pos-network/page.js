'use client'
import PublicNav from '@/components/PublicNav'
import { useState } from 'react'
export default function POSNetwork(){const[form,setForm]=useState({status:'Planning'});const[msg,setMsg]=useState(null);const set=(k,v)=>setForm({...form,[k]:v});async function submit(e){e.preventDefault();const r=await fetch('/api/financial-service-request',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...form,type:'pos_network'})});const b=await r.json();setMsg(`Request ${b.request_no} captured for POS/device review.`)}return <><PublicNav/><main className="mx-auto max-w-5xl px-5 py-10"><a href="/services" className="font-black text-purple-800">← Back to Services</a><form onSubmit={submit} className="card mt-6 p-8"><p className="font-black uppercase tracking-widest text-purple-700">AILIFE POS Network</p><h1 className="mt-2 text-4xl font-black text-purple-950">POS terminal and agent network setup</h1><p className="mt-3 text-slate-600">Track custom POS manufacturing partners, terminal inventory, agent assignment, branch deployment, maintenance, settlement and fraud monitoring.</p><div className="mt-8 grid gap-4 md:grid-cols-2"><Field label="Manufacturer / Vendor" v={form.vendor} set={v=>set('vendor',v)} ph="China OEM / Terminal provider"/><Field label="Terminal Model" v={form.model} set={v=>set('model',v)} ph="Android POS / MPOS"/><Field label="Quantity" v={form.quantity} set={v=>set('quantity',v)} ph="100"/><Field label="Target Branch / Area" v={form.branch} set={v=>set('branch',v)} ph="Osun / Ibadan / PH"/><Field label="Agent Name" v={form.agent} set={v=>set('agent',v)}/><Select label="Status" v={form.status} set={v=>set('status',v)} opts={['Planning','Vendor Review','Compliance Review','Procurement','Deployed','Suspended']}/></div><button className="btn btn-gold mt-6">Create POS Network Request</button>{msg&&<p className="mt-4 rounded-xl bg-emerald-50 p-4 font-bold text-emerald-800">{msg}</p>}</form></main></>}function Field({label,v,set,ph='',req=false}){return <div><label className="text-sm font-black">{label}</label><input className="input mt-2" value={v||''} onChange={e=>set(e.target.value)} placeholder={ph} required={req}/></div>}function Select({label,v,set,opts}){return <div><label className="text-sm font-black">{label}</label><select className="input mt-2" value={v} onChange={e=>set(e.target.value)}>{opts.map(o=><option key={o}>{o}</option>)}</select></div>}
