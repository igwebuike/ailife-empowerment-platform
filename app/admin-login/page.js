'use client'
import { LoginForm } from '../login/page'
export default function AdminLogin(){return <LoginForm title="Admin Login" portal="admin" redirect="/dashboard" subtitle="Executive, finance and system administration access with 2FA protection."/>}
