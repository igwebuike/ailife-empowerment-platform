'use client'
import PublicNav from '@/components/PublicNav'
import { useState } from 'react'
export default function Login(){return <LoginForm title="Staff Login" portal="staff" subtitle="Secure access for Credit Officers, Branch Managers, Area Managers, Accountants and operations staff." redirect="/dashboard"/>}
export function LoginForm({title,subtitle,portal,redirect}){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[otp,setOtp]=useState('');const[msg,setMsg]=useState('');async function submit(e){e.preventDefault();setMsg('');const res=await fetch('/api/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password,otp,portal})});const body=await res.json().catch(()=>({}));if(!res.ok){setMsg(body.error||'Login failed')}else{location.href=body.needs_2fa_setup?'/dashboard/settings':redirect}}return <><PublicNav/><main className="mx-auto grid min-h-[80vh] max-w-6xl place-items-center px-5"><form onSubmit={submit} className="card w-full max-w-md p-8"><h1 className="text-3xl font-black text-purple-950">{title}</h1><p className="mt-2 text-slate-600">{subtitle}</p><label className="mt-6 block text-sm font-bold">Email</label><input className="input mt-2" value={email} onChange={e=>setEmail(e.target.value)} type="email" required/><label className="mt-4 block text-sm font-bold">Password</label><input className="input mt-2" value={password} onChange={e=>setPassword(e.target.value)} type="password" required/><label className="mt-4 block text-sm font-bold">Authenticator Code</label><input className="input mt-2" value={otp} onChange={e=>setOtp(e.target.value)} placeholder="6-digit code after 2FA setup" inputMode="numeric"/><button className="btn btn-primary mt-6 w-full">Continue</button><p className="mt-3 text-xs text-slate-500">New users without an authenticator secret will be guided to set up 2FA after their first login.</p>{msg&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{msg}</p>}</form></main></>}
